on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  formats:
    name: Check format
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-dotnet@v1
      - name: CheckFormat
        run: |
         dotnet tool install -g dotnet-format
         dotnet format --check --folder PartsTreeSystem/
         dotnet format --check --folder PartsTreeSystemTest/
         dotnet format --check --folder PartsTreeSystemExample/
        shell: sh

      - name: Test
        run: |
         dotnet build PartsTreeSystemTest/
         dotnet test PartsTreeSystemTest/
        shell: sh
